#include<stdio.h>
#include "MyroC.h"

int main()
{
  /*
  int quarts = 1;
  double liters;
  printf ("Table of quart and liter equivalent\n");
  printf("Quarts        Liters\n");
  // for loop for iterations of quarts

    for (quarts = 1; quarts <= 12; quarts++)
    {
      liters = quarts * .9463;
      printf("%4d%16.4lf\n", quarts, liters);
    }
  printf("\n");
  

  //while loop for iterations of quarts
  do  
    {liters = quarts * .9463;
      printf("%4d%16.4lf\n", quarts, liters);
      quarts++;
}
  while (quarts <= 12);

  */
  /* int i;
  rConnect ("/dev/rfcomm0");
  for (i = 1; i <= 5; i++)
    {
      rForward (.5, 1);
    }


    }*/
